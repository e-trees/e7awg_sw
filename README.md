# e7awg_sw

## Requirements

- Python3
- pipenv
- pyenv
  - libssl-dev (Ubuntu 20.04)

## Setup

```
$ pipenv shell
$ pipenv install
```

If you don't have Python 3.9.10, the Python will be installed by `pyenv`.






